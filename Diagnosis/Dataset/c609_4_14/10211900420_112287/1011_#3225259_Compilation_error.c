#include<stdio.h>
#include<string.h>
double pow
int main()
{
    char a[100],b[100],c[100];
    double sum;
    int j=0,i,len;
    scanf("%s",&a);
    len=strlen(a);
    for(i=0;i<len;i++)
    {
        if(a[i]!='.')
        {
            b[i]=a[i];
        }
        else
        {
            break;
        }
    }
    lenb=strlen(b);
    for(int m=0;m<i;m++)
    {
        if(b[m]=='1')
            sum=sum+
    }
    for(i=i+1;i<len;i++)
    {
        c[j]=a[i];
        j++;
    }
    return 0;
}