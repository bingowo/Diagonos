#include <stdio.h>
#include <stdlib.h>
#include<string.h>
#define N 100
int main()
{
    int A,B,j=0,len=0;
    char s[N];
    scanf("%d",&A);
    scanf("%s",s);
    scanf("%d",&B);
    len=strlen(s);
    printf("%d",len);
    return 0;
}

