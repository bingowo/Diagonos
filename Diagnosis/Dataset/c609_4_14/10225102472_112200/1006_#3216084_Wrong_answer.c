#include <stdio.h>
#include <string.h>

int main()
{
    int T;
    char x[61];
    
    scanf("%d", &T);
    
    for (int k = 0; k < T; k++)
    {
        scanf("%s", x);
        
        int len = strlen (x);
        
        
    }
}