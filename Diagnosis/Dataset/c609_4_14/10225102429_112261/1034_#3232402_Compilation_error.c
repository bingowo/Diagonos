#include <stdio.h>
#include <string.h>
#define N 100005
char s[N];
int main()
{
    scanf("%s",s);
    int ret = 0;
    int len = strlen(s);
    for(int i = 0; i<len; i++)
    {
        if(a[i]=='0')
        {
            if(i<len-1){
                if(a[i+1]=='x'){
                    ret = 1;
                    while(ret)
                    {
                        
                    }
                }
            }
        }
    }
    return 0;
}