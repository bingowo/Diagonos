#include <stdio.h>
#include <stdlib.h>
void drepeat(void) //去除数组中重复的数
{
    int i,j,n;
    for(i=0;scanf("%d",&n)!=EOF;i++)
    {
        j=
            
    }
}
int cmpA(const void *a,const void *b)
{
    int *s1,*s2;
    s1=(int *)a;
    s2=(int *)b;
    while(*s1!=-1&&*s2!=-1&&*s1==*s2) s1++,s2++;
    return *s1-*s2;
}
int cmpB(const void *a,const void *b)
{
    int *s1,*s2;
    s1=(int *)a;
    s2=(int *)b;
    while()
}