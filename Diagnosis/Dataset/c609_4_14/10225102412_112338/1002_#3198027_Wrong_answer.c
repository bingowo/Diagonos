#include<stdio.h>
int main()
{
    int T,n;
    scanf("%d",&T);
    for (int i=0;i<T;i++){
        scanf("%d",&n);
        printf("case #%d:\n%d\n",i,casecase(n));
    }
    return 0;
}

int casecase(int n)
{
    
    return 0;
}