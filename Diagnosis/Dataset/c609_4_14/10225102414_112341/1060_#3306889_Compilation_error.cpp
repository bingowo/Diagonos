#include <iostream>
#include <string>
#include <algorithm>
const int N{100};
using namespace std;

string R(string s)
{
    if (s[0]){
        if (s[0] + s < s) s = t;
    }
    return s;
}
