#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct{
    
    
    
}student;

int cmp(const void* a, const void* b)
{
    
    
    
    
}

int main()
{
    int T, i, n, j;
    int t, k, m, g;
    scanf("%d", &T);
    for(i=0; i<T; i++)
    {
        scanf("%d%d%d", &n, &m, &g);
        int score[m];
        
        
        
        
        
        
    }

    return 0;
}
