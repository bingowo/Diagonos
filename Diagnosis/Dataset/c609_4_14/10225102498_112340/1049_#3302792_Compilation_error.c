#include <stdio.h>
#include <stdlib.h>
#define N 1000

int main()
{
    int a[500], b[500];
    char a[501] = {0};
    char b[501] = {0};
    while(scanf("%s%s", a, b) != EOF)
    {
        int lena = strlen(a);
        int lenb = strlen(b);
        int i;
        for(i=lena; i>=0; i--)
            
        
        
        
        
        
        
        
        
        
    }
    

    return 0;
}
