#include<stdio.h>
#include<string.h>
int main()
{
    int t;
    char s;
    scanf("%d%s",&t,s);
    for(int i=0;i<t;i++){
        printf("case #%d:\n",i);
        printf("%d\n",f(s));
    }
    return 0;
}

void f(char s)
{
    int a;//转化后的结果
    while((c=getchar())!=0){
        
    }
    
    return a;
}