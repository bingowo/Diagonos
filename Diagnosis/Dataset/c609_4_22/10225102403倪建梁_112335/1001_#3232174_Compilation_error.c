#include<stdio.h>
int main()
{
    int T;
    scanf("%d", &T);
    char table[]="0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    for (int i=0;i,T;i++)
    {
        int N,R,k=0,sign=1,a[33];
        scanf("%d%d",&N,%R);
        if (N<0) {sign=-1;N=-N;}
        do{
            a[k++]=table[r];
            N=n/R;
        }while(N);
        if(sign==-1) prinft("-");
        for(k=k-1;k>=0;k--)printf("%c",a[k]);
        printf("\n");
    
    return 0
}