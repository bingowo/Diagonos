#include<stdio.h>
#include<stdlib.h>
#include<string.h>

typedef struct{
    int year;
    char month[6];
    int day;
}calendar


int main()
{
    int T, N, i, j;
    scanf("%d", &T);
    for(i=0; i<T; i++)
    {
        scanf("%d", &N);
        char** mon = (char**)malloc(19*sizeof(char*));
        for(j=0; j<19; j++)
            mon[j] = (char*)malloc(6*sizeof(char));
        m
        
        for(j=0; j<N; j++)
        
        
        
        
        
        
        
        
        
        
        
        printf("case #%d:\n", T);
        
        
        
    }

    return 0;
}
