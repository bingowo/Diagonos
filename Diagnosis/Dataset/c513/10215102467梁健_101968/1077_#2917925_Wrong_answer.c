#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<ctype.h>
#include<math.h>
int main()
{
    int m,n;
    scanf("%d",&m);int A[m+1];
    for(int i=0;i<m;scanf("%d",&A[i++]));
    scanf("%d",&n);int B[n+1];
    for(int i=0;i<n;scanf("%d",&B[i++]));
    return 0;
}
