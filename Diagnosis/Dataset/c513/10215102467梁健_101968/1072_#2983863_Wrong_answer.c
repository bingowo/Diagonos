#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<math.h>
#include<ctype.h>

int main()
{
    long long x,y,step=1,time;scanf("%lld %lld",&x,&y);
    if(x%2&&y%2)printf("-1");
    else if(x%2==0&&y%2==0)printf("-1");
    else{

    }
    return 0;
}
