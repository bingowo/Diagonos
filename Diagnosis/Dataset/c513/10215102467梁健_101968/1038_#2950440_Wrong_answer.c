#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<math.h>
#include<ctype.h>
int main()
{
    int n,m,k;scanf("%d %d %d",&n,&m,&k);
    int list[n][m];
    for(int i=0;i<n;i++)for(int p=0;p<m;p++)scanf("%d",&list[i][p]);
    
    return 0;
}
