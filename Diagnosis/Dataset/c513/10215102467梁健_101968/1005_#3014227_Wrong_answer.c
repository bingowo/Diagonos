#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<math.h>
#include<ctype.h>

int main()
{
    int t;scanf("%d",&t);
    for(int T=0;T<t;T++){
        printf("case #%d:\n");
        char s[60];scanf("%s",s);
        int n[200],res[200]={0};
        int len=strlen(s);
        printf("0.");
        for(int i=2;i<len;i++){
            
        }
    }
    return 0;
}