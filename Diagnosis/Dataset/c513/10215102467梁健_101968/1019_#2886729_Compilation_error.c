#include<stdio.h>
#include<string.h>
int main()
{
    int m,n;
    scanf("%d %d",&n,&m);
    char s[n];
    for(int i=0;i<n;i++)s[i]=0;
    for(int i=0;i<m;i++){
        int a,b,sit;
        scanf("%d %d %d",&a,&b,&sit);
        if (c==1)s[a-1]+=3,s[b-1]-=1;
        if (c==0)s[a-1]+=1,s[b-1]+=1;
        else s[a-1]-=1,s[b-1]+=3;
    }
    qsort
    return 0;
    
}