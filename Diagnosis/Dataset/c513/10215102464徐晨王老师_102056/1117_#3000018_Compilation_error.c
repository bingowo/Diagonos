#include <stdio.h>
#include <stdlib.h>
#include <math.h>
struct cds{
    long long int x;
    long long int y;
    unsigned long long int len;
}

int main(){
    long long int x0,y0;
    scanf("%lld%lld",&x0,y0);
    int n;
    struct cds p[n]; 
    for(int i=0;i<n;i++){
        scanf("%lld%lld",p[i].x,p[i].y);
        if()
    }
}