#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main(){
    char p[1000];
    int len=0;
    while((p[len]=getchar())!=EOF){
        len=len+1;
    }
    for(int i=0;i<len;)
    return 0;
}