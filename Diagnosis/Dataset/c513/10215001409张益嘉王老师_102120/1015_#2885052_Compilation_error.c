#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void readnum(char s[],int a[])
{
    
}

int main()
{
   int i,t;
   scamf("%d",&t);
   for(i=0;i<t;i++)
   {
       int len1,j,k=0,len2;
       char s1[60],s2[60];
       int a[100],b[100];
       char s[1000]={0};
        scanf("%s%s",s1,s2);
       len1=strlen(s1);
       len2=strlen(s2);
       for(j=0;j<len1;j++)
       {
            if (s1[i]) a[i]=s1[i]-'0';
       }
   }
}
