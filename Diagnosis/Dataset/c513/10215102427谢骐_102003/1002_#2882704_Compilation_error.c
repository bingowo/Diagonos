#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
	int tab[128];
	int 
	for(int i = 0; i < 128; ++i)
		tab[i] = -1;

	scanf("%d", &t);

	for (int i = 0; i < t; ++i) {
		scanf("%s", s);
		n = strlen(s);
		tab[s[0]] = 1;
		weight = 1;
	
	}

	return 0;
}