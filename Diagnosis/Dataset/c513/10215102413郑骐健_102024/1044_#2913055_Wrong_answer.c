#include <stdio.h>
#include <stdlib.h>
#include <string.h>





int main()
{
    unsigned long long a =0;
    char*p = (char*)malloc(sizeof(char)*1e6);
    scanf("%s",p);
    int length = strlen(p);
    for(int i =0;i<length;i++)
    {
        
    }
    return 0;
}   
