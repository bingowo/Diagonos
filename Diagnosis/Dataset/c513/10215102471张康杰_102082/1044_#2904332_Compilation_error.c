#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    char input[100001];
    scanf("%s",&input);
    char table
    char*pt;
    while(strstr(input,"0x") != NULL)
    {
        pt = strstr(input,"0x") != NULL;
        
    }
    return 0;
}
