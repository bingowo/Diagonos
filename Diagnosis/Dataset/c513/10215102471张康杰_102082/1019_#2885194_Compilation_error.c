#include <stdio.h>
#include <stdlib.h>
typedef struct
{
    int score;
    int a;
}Tea;
int main()
{
    int n,m;
    while(scanf("%d %d",&n,&m) != EOF)
    {
        Tea team[n]
        int a,b,c;
        for(int i = 0;i < n;i++)
        {
            team[i].score = 0;
            team[i].a = i + 1;
        }
        for(i = )

    }
    return 0;
}
