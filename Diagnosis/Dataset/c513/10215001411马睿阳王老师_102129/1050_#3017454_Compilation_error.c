#include<stdio.h>
#include<string.h>

int main()
{
   char s1,s2,s;
   int i=0,j=0,k=0,t=0,T,l1=0,l2=0,l3=0;
   scanf("%d",&T);
   for(i=0;i<T;i++)
   {
       scanf("%s",s1);
       scanf("%s",s2);
       scanf("%s",s);
       l1=strlen(s1);
       l2=strlen(s2);
       l=strlen(s);
       printf("case #%d:\n",i);
       for(j=0;j<l;j++)
       {
           for(k=0;k<l1;k++)
           {
               if()
           }
       }
       
   }
}
