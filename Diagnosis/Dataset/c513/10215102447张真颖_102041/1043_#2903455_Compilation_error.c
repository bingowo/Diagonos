#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int max_erase(char s[])
{
    char t[102]="";
    int len=strlen(s);
    for(int i=0;i<len;i++)
    {
       
    }
}

int main()
{
    int Y=0;
    scanf("%d",&T);
    getchar();
    for(int i=0;i<T;i++)
    {
         char s[101]="";
         gets(s);
         
    }
    return 0;
}
