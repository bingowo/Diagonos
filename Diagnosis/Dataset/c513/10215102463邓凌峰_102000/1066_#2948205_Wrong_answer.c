#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int ways(int p,int m,int t,int tmp,int cnt){
    if(tmp==t) return cnt;
}

int main()
{
    int cas;scanf("%d",&cas);
    for(int i=0;i<cas;i++){
        int n,p,m,t;scanf("%d%d%d%d",&n,&p,&m,&t);

    }

}
