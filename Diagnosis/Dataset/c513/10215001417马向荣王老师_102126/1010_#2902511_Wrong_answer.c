#include<stdio.h>
#include<string.h>
int main(void)
{
    char arr[500];
    scanf("%s",arr);
    int len=strlen(arr);
    printf("0001");
    for(int i=0;i<len/3;i++)
    {
        
    }
    return 0;
}