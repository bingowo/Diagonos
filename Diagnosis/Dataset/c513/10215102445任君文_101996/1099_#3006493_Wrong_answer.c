#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#define M 1000
int main()
{
    int T;
    int i,j;
    int L=0;
    char str[M]={0};
    scanf("%d",&T);
    for(i=0;i<T;i++)
    {
        scanf("%s",str);
        L=strlen(str);
        for(j=0;j<L;j++)
        {
            ;
        }
        printf("case #%d:\n",i);
    }
    return 0;
}