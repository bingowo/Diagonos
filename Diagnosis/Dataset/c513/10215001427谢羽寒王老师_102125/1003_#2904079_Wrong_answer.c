#include<stdio.h>
#include<string.h>

int main()
{
    int T,N,Y=1000;
    int i,j,x,y,result,r,z,c;
    int s;
    int a[Y];
    scanf("%d",&T);
    for(i=0;i<T;i++){
        scanf("%d",&N);
        for(j=0;j<N;j++){
        scanf("%d ",&a[j]);
        //先把十进制转成二进制 算1的个数
        
        
        }

        printf("case #%d:\n",i);
        printf("%d\n",c);
    }






   return 0;
}
