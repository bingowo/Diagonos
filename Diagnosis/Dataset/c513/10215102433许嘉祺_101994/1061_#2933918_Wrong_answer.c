#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main()
{
    int n;
    scanf("%d",&n);
    char s[10000][16];
    for (int i=0;i<n;i++){
        scanf("%s",s[i]);

    }
    return 0;
}
