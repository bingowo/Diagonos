#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{ int sum=0,n,i,j,m,s,w[110];
  int s[111100];
  scanf("%d",&n);
  for(i=0;i<n;i++)
   {scanf("%d",&w[i]); sum+=w[i];
   } 
   for(j=0;j<sum;j++)
   {  
       
   }
}