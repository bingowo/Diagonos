#include<stdio.h>
#include<stdlib.h>
char* gettime(char* a)
{
    
}
struct file
{ char time[20];
  int size;
  char  name[100];
    
};
int cmp(const void*a,const void*b)
{
    
}
int main()
{ int n,i; char s[102][100000]; struct file a[10000];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {gets(s[i]);
    a[i].time=gettime
        }
    
}