#include <stdio.h>
#include <string.h>
int main()
{
    int cnt=0;
    scanf("%d",&cnt);
    for(int k=0;k<cnt;k++)
    {
        char s[101];
        scanf("%s",s);
        int R=0,G=0,B=0,Y=0,len=(int)strlen(s);
        
    }
}