#include <stdio.h>
#include <stdlib.h>
int cmp(const void *a,const void *b)
{

}
int main()
{
    int cnt=0;
    scanf("%d",&cnt);
    for(int i=0;i<cnt;i++)
    {
        int num=0;
        scanf("%d",num);
        int **L=(int**)malloc(num*sizeof(int*));
        for(int k=0;k<num;k++)
        {
            int L[50]={0};
            for(int t=0;L[t]!='-1';t++) scanf("%d",&L[t]);
        }
        
    }
}