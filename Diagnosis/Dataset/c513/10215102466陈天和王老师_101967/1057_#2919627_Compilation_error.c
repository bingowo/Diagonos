#include <stdio.h>
#include <string.h>
#include <stdlib.h>
typedef struct Lnode{
    char ch;
    struct Londe* next;
}LNode,*LinkList;

void priorInsert(LNode *it, char ch){
    LNode *tmp;
    tmp=(LNode*)malloc(sizeof(LNode));
    tmp->char
}
int main(){
    char str[1001];

    scanf("%s",str);
    for(int i=0;i<strlen(str);i++){

    }
}//等学习了数据结构的链表，尝试用前插法和尾插法解决它