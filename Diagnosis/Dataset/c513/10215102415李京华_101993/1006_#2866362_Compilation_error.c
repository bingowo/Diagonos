#include <stdio.h>
#include <string.h>

int main()
{
	int i, T;
	scanf("%d",&T)
	for (i = 0; i < T; i++){
		char s[100];
		scanf("%s",s);
		int len = strlen(s);
		
		int j;
		for (j = 0; j < len; j++){
			
		}
		
		
		
		printf("case #%d:\n",i);
		printf("%d\n",ans);
	}
	return 0;
}