#include<stdio.h>
#include<stdlib.h>
int cmp(const void *pa,const void *pb)
{
    int a,b;
    a=*((int *)pa);
    b=*((int *)pb);
    return a-b;
}
void tidy(int a[1000])
{
    for(int i=0;i<1000;i++)
    {
        for(int j=i+1;j<1000;j++)
        {
            if()
        }
    }
}
int main()
{
    char x;
    scanf("%c",&x);
    int a[1000];
    scanf("%d",a);
    if(x=='A')
    {
        
    }
}