#include<stdio.h>
#include<string.h>
struct data
{
    int R;
    int B;
    int Y;
    int G;
};
int main()
{
    int number;
    scanf("%d",&number);
    for(int i=0;i<number;i++)
    {
        char s[100];
        scanf("%s",&s);
        int l=strlen(s);
        
    }
}