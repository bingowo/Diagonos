#include<stdio.h>
#include<stdlib.h>
int main()
{
    int number;
    scanf("%d",&number);
    for(int i=0;i<number;i++)
    {
        int N;
        int M;
        int G;
        int question[M];
        for(int j=0;j<M;j++)
        {
            scanf("%d",&question[j]);
        }
        char s[N][11];
        for(int k=0;k<N;k++)
        {
            scanf("%s",&s[k]);
            int n;
            scanf("%d",&n);
            int ti[n];
            for(int q=0;q<n;q++)
            {
                scanf("%d",&ti[q]);
            }
        }
        int x[N];
        
    }
}