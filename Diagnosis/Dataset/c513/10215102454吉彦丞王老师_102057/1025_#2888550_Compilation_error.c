#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int n(char s[])
{
    int d=1;
    int l=strlen(s);
    for(int i=0;i<l;i++)
    {
        
    }
}
int main()
{
    int number;
    scanf("%d",&number);
    int a[number];
    for(int i=0;i<number;i++)
    {
        scanf("%d",&a[i]);
        char s[a[i]][100];
        int l[a[i]];
        for(int j=0;j<a[i];j++)
        {
            scanf("%s",s[j]);
            l[j]=strlen[s[j]];
            
        }
    }
}