#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main()
{   int m,n,a[200][2],b[n],c[n];
    scanf("%d %d",&n,&m);
    for(int i=0;i<=n-1;i++)
    { scanf("%d %d",&a[i][0],&a[i][1]);
      b[i]=a[i][0]*a[i][0];
      c[i]=2*a[i]a[0]*a[i][1];
    }
    if(a)

}
