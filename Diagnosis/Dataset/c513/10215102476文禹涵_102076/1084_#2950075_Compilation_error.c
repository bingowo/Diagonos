#include <stdio.h>
#include <math.h>

int main()
{
	int t;
	scanf("%d", &t);
	int N;
	for(int i = 0 ; i < t ; i++){
		scanf("%d", &N);
		printf("case #%d:\n%.0f\n", i, pow(2, input));
	}
		
	return 0;
}
