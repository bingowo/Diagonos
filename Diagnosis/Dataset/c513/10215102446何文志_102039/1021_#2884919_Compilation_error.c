#include <stdio.h>
#include <stdlib.h>

int main()
{
    char s[101];memset(s,0,101*sizeof(char));
    while(scanf("%s",s)!=EOF)
    {
        char* p[101];memset(p,0,101*sizeof(char*));
        char word[21];memset(word,0,21*sizeof(char));
        int i = 0;
        while(word(scanf("%s",word[i++])!=0)
        {
            
        }

    }
    return 0;
}
