#include<stdio.h>
#include<string.h>
#include <stdlib.h>
int main(){
    int T,i;
    scanf("%d",&T);
    for(i=0;i<T;i++){
        int b1,b2,b3,b4,b5,b6,b7,b8,b9;
        int a[3][3]={1};
        scanf("%d %d %d",&b1 &b2 &b3);
        scanf("%d %d %d",&b4 &b5 &b6);
        scanf("%d %d %d",&b7 &b8 &b9);
        if(b1%2==1){
            if(a[0][0]==1){
                a[0][0]=0;
            }else{
               a[0][0]=1; 
            }
            if(a[0][1]==1){
                a[0][1]=0;
            }else{
                a[0][1]==1
            }
            if(a[])
        }
        
    }
}