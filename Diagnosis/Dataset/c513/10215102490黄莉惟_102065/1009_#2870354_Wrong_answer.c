#include<stdio.h>
#include<string.h>
int main(){
    int n,i,j,num,num1;
    char s[300];
    scanf("%d",&n);
    int m=0;
    while(m<n){
        num=0;
        scanf("%s",s);
        printf("%d",strlen(s));
        m++;
    }
}