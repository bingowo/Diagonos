#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<math.h>
int main()
{
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        char s[110];
        scanf("%s",s);
        int k=0,flag=1;
        switch(s[k])
        {case'(':
            
        }
    }
}