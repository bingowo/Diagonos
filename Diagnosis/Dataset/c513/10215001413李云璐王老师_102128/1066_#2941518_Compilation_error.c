#include<stdio.h>
#include<string.h>
#include<string.h>
#include<math.h>
int main()
{
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        int n,p,m,t,data,duo;
        scanf("%d%d%d%d",&n,&p,&m,&t);
        if(fabs(p-t)==m) data=1;
        else ((fabs(p-t)-m)%2==0) 
        
        
    }
}