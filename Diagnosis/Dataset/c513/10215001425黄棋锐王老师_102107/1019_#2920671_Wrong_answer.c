#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>		
int main(){
		int n,m;
		while(scanf("%d %d",&n,&m)&&(n!=0||m!=0))
		{
		int score[12]={0};
		int i,a=0,b=0,c=0;
		for(i=0;i<m;i++){
			scanf("%d %d %d",&a,&b,&c);
			
		}
	}
		return 0;
}