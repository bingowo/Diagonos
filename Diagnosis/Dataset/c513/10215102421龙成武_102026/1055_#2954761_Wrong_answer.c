#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    int hash[256];
    memset(hash,0,sizeof(hash));
    for(int i=0;i<1000001;i++)
    {
        printf("?");
    }
    return 0;
}