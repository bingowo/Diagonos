#include<stdio.h>
#include<malloc.h>
int main(){
	int n,i,j;
	int a[128];
	scanf("%d",&n);
	int *p=NULL;
	p=(int*)malloc(sizeof(int)*n);
	char s[61];
	for(i=0;i<n;i++){
		scanf("%s",&s);
		//每次读入字符串进行一次运算把答案存储在p中 
		while(s[j]!='\0'){
			
		}
	}
}