#include<stdio.h>
#include<stdlib.h>
#include<ctype.h>

int main(){
    int t,a=0,num[90];
    char s[110];
    scanf("%d",&t);
    while(a<t){
        memset(num,0,sizeof(num));
        scanf("%s",s);
        
        printf("case #%d:\n",a);
        a++;
        
    }
}