#include <stdio.h>
#include <stdlib.h>
#include<string.h>
int main()
{
    int n,m,num,k;
    scanf("%d\n",&n);
    for(k=0;k<n;k++){
        scanf("%d\n",&m);
        char s[m][];
        while(scanf("%s"))
    }
}