#include <stdio.h>
#include <stdlib.h>
#include<string.h>
int cnt(int x){
   int i,j;
   for(i=0;x>0;{
      
   }
}
int main()
{
    int n,c;
    char s[120];
    scanf("%d\n",&n);
    for(;n>0;n--){
       scanf("%s\n",s);
       c=sizeof(s);
       int num=0;
       for(int i=0;i<c;i++){
       num+=cnt(atoi(s[i]));
         
       }
    }
}