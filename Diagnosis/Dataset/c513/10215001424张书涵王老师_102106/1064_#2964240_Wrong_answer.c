#include <stdio.h>
#include <stdlib.h>

int main()
{
    int T;
    scanf("%d",&T);
    int i;
    for(i=0;i<T;i++)
    {
        char s1[1000],s[1000];
        gets(s1);
        int j,p;
        for(j=0,p=0;s1[j];j++)
        {
            if(s1[j]==' ');
            else
                s[p++]=s1[j];
        }
        int l;
        l=strlen(s);
        for(j=(l-1);j>=0;j--)
        {

        }
    }
}