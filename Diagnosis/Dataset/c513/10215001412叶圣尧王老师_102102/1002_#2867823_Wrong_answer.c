#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
    int n,number=0,len;char message[100];int put[100];
    scanf("%d",&n);
    while(scanf("%s",message)!=EOF)
    {
        printf("case #%d",number++);
        put[0]=1;
        len=strlen(message);
        
        
    }
}